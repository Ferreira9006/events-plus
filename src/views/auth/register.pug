extends ../layouts/main

block content
  section(class="bg-white shadow-lg rounded-xl p-8")
    h1(class="text-2xl font-bold text-center mb-6 text-slate-800") Criar Conta

    form(action="/auth/register" method="POST" class="flex flex-col gap-5")
      
      // NOME
      div(class="flex flex-col gap-1")
        label(for="name" class="text-sm font-medium text-slate-700") Nome completo
        input#name(
          type="text"
          name="name"
          required
          placeholder="Ex: João Silva"
          class="border border-slate-300 rounded-lg px-3 py-2 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500"
        )

      // EMAIL
      div(class="flex flex-col gap-1")
        label(for="email" class="text-sm font-medium text-slate-700") Email
        input#email(
          type="email"
          name="email"
          required
          placeholder="email@exemplo.com"
          class="border border-slate-300 rounded-lg px-3 py-2 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500"
        )

      // PASSWORD
      div(class="flex flex-col gap-1")
        label(for="password" class="text-sm font-medium text-slate-700") Password
        input#password(
          type="password"
          name="password"
          required
          placeholder="********"
          class="border border-slate-300 rounded-lg px-3 py-2 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500"
        )

      // ROLE (opcional)
      div(class="flex flex-col gap-1")
        label(for="role" class="text-sm font-medium text-slate-700") Tipo de conta
        select#role(
          name="role"
          class="border border-slate-300 rounded-lg px-3 py-2 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500"
        )
          option(value="PARTICIPANT" selected) Participante
          option(value="ORGANIZER") Organizador
          option(value="ADMIN") Administrador

      // BOTÃO
      button(
        type="submit"
        class="w-full mt-2 bg-blue-600 text-white py-2.5 rounded-lg font-medium hover:bg-blue-700 transition-colors"
      ) Criar Conta

    p(class="text-center text-sm text-slate-600 mt-4")
      | Já tens conta?
      a(href="/auth/login" class="text-blue-600 font-medium hover:underline ml-1")
        | Inicia sessão aqui
